extend type Mutation @group(middleware: ["auth:api"]) {
    UnFollowAccount(
        id: Int! @rules(apply: ["required", "integer", "unique:accounts,id"])
    ): Boolean @field(resolver: "UnFollowAccountResolver@resolve")
}
