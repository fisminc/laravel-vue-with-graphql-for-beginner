extend type Mutation @group(middleware: ["auth:api"]) {
    UpdateAccount(
        name: String @rules(apply: ["required", "string", "max:40"])
        avatar: String
    ): Account @field(resolver: "UpdateAccountResolver@resolve")
}
